/* Mandarin The SenLin Way — Phase 6 · HSK 6  (see hsk1.js for the format) */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else (root.SENLIN_LEVELS = root.SENLIN_LEVELS || []).push(factory());
})(this, function () {
  'use strict';
  return {
    level: 6,
    name: 'HSK 6',
    components: [],
    characters: [
    ],
    words: [
    ],
    sentences: [
    ]
  };
});
