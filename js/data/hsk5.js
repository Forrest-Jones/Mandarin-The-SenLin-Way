/* Mandarin The SenLin Way — Phase 5 · HSK 5  (see hsk1.js for the format) */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else (root.SENLIN_LEVELS = root.SENLIN_LEVELS || []).push(factory());
})(this, function () {
  'use strict';
  return {
    level: 5,
    name: 'HSK 5',
    components: [],
    characters: [
    ],
    words: [
    ],
    sentences: [
    ]
  };
});
