/* Mandarin The SenLin Way — Phase 3 · HSK 3  (see hsk1.js for the format) */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else (root.SENLIN_LEVELS = root.SENLIN_LEVELS || []).push(factory());
})(this, function () {
  'use strict';
  return {
    level: 3,
    name: 'HSK 3',
    components: [],
    characters: [
    ],
    words: [
    ],
    sentences: [
    ]
  };
});
