{
  "name": "senlin-native",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell (Android + iOS) for Mandarin The SenLin Way. The web app in ../ is copied into www/ at build time.",
  "scripts": {
    "build": "node scripts/copy-web.js",
    "sync": "npm run build && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios",
    "run:android": "npm run sync && npx cap run android",
    "run:ios": "npm run sync && npx cap run ios",
    "add:android": "npx cap add android",
    "add:ios": "npx cap add ios"
  },
  "dependencies": {
    "@capacitor-community/speech-recognition": "^7.0.1",
    "@capacitor-community/text-to-speech": "^8.0.2",
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.2",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.5.2",
    "@capacitor/local-notifications": "^8.3.1",
    "@capacitor/share": "^8.0.2",
    "@revenuecat/purchases-capacitor": "^13.6.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  },
  "engines": {
    "node": ">=20"
  }
}
